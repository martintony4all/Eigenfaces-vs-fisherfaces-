README


1. The text file "Source_code.txt" contains the implementation code.

2. The implementation code is run in python. Use jupyter notebook to run chunks of code for better 
   computer interactivity.

3. The folder "dataset" contains the directory for the ORL face images

4. In the dataset folder, the directory "test" and "training" is used for the eigenfaces PCA approach

5.In the dataset folder, the directory "test" and "training11" is used for the fisherfaces LDA method
  as seen in the sourcecode
 
